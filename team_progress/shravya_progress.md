Week - 1 Performed data cleaning on the Orders dataset from the Brazilian E-Commerce Olist dataset.

Steps completed:

• Imported the raw orders dataset into Excel for inspection.

• Explored the dataset and reviewed all columns including order_id, customer_id, order_status, and timestamp fields.

• Checked for duplicate order_id values using Excel to ensure each order record is unique.

• Verified that order_id values are unique 

• Identified some missing values in timestamp fields such as order_approved_at, order_delivered_carrier_date, and order_delivered_customer_date.
